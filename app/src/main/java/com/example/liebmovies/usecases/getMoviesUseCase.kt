package com.example.liebmovies.usecases

class getMoviesUseCase {
    //todo , this is in order to prevent usage if application context in viewModel,
    // and instead inject the usecases in the view model for every user action
}